# Longitudinal DGA dataset — thumbnail

Drop ONE image here: the dataset thumbnail shown on the IEEE DataPort page.
It is auto-discovered (first file used) and shown on `/datasets`.

- Any name, e.g. `thumb.png`.
- Formats: png / jpg / jpeg / webp / avif.
